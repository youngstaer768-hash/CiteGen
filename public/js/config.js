/* CiteGen API base URL
   Same-origin works both on Netlify and local Express dev.
   Optional override: set window.__CITEGEN_API_BASE before this file loads.
*/

window.API_BASE = window.__CITEGEN_API_BASE || '';
